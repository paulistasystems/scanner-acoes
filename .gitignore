venv*/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so-
*.egg-info/
dist/
build/
Legacy Scanner é claramente superior**.md
log

# SQLite do data_layer (artefato de runtime)
scanner.db
scanner.db-shm
scanner.db-wal

# Runtime: logs do warm_cron, restart.txt do Passenger, etc.
tmp/

# Logs baixados do servidor (./remote_logs.sh) — nunca commitar / nunca deployar
remote_logs/

# Artefatos de sessão do Claude Code
EnterPlanMode*
tools/_*.json

# Logs/transcrições de sessão (artefatos de runtime) — requirements.txt segue versionado
*.txt
!requirements.txt

# Secrets do Supabase (gestão dinâmica de símbolos) — NUNCA commitar
.env
.streamlit/secrets.toml

# PHP Cache/Snapshots (diagnostic dumps & lock)
php/all_data.csv
php/all_data.json
php/.snapshot.lock

# Docker local (volume nomeado é o default; pasta se alguém montar bind)
docker-data/

# DB exportado do volume Docker (warm local → upload opcional)
scanner.docker.db
scanner.docker.db-*

# Passenger Standalone (container bind-mount)
passenger.*.pid
passenger.*.log
